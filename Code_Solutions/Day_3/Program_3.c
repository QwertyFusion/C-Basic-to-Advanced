/* Question 3:
WAP to use define statement in order to define a function of adding two numbers.
*/

#include <stdio.h>
#define add(x,y) x+y

int main()
{
    printf("addition of two numbers: %d", add(4,3));
    return 0;
}
